import React, { Component } from 'react'

export default class Items extends Component {
    
    render() {
        return (
        <div>
            <h1>This is where we can show the Items</h1>
        </div>
        )
    }
}
